const { defineFeature, loadFeature } = require('jest-cucumber');

// C'est ici que le Collègue 3 devra travailler.
// Exemple : const feature = loadFeature('../../features/task_validation.feature');
